- functions in js dont require their argument's data type to be declared as it is a dynamically typed language 
- `${}` is used for string interpolation which means embedding variables and expressions inside a string and it is used with template literals/backticks 
- `export` keyword is used to make that function accessible to other files and modules. similar to access specifiers in java but js has private as default
- regex is used to identify the pattern in the url to check if the webpage is for a question or not 
- `attachShadow` is an inbuilt method so gotta learn more about this 
- DOM can be manipulated by adding new elements and organising them using built in method
- query selector method require the `.` to indicate that it should search for a class name and similarly `#` for an ID name and if nothing is used then it is to select a HTML tag 

### Debugging techniques 
- the universal logging onto console but the thing to keep in mind here is that there are two different consoles : page's DevTools console which is for scripts that are injected | service worker's own console accessible from the extension site 
- DOM and their hierarchy can be checked as well with respect to frontend and there is a breadcrumb trail which shows the hierarchy of the elements of DOM 